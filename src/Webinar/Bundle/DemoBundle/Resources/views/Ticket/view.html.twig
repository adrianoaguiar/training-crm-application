{% extends 'OroUIBundle:actions:view.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as macros %}

{% oro_title_set({params : {"%ticket.eventName%": entity.eventName} }) %}

{% block navButtons %}
    {% if resource_granted('EDIT', entity) %}
        {{ UI.editButton({
            'path' : path('webinar_demo_ticket_update', { 'id': entity.id }),
            'entity_label': 'webinar.ticket.entity_label'|trans
        }) }}
    {% endif %}
    {% if resource_granted('DELETE', entity) %}
        {{ UI.deleteButton({
            'dataUrl': path('webinar_api_delete_ticket', {'id': entity.id}),
            'dataRedirect': path('webinar_demo_ticket_index'),
            'aCss': 'no-hash remove-button',
            'id': 'btn-remove-ticket',
            'dataId': entity.id,
            'entity_label': 'webinar.ticket.entity_label'|trans
        }) }}
    {% endif %}
{% endblock navButtons %}

{% block stats %}
    <li>{{ 'oro.ui.created_at'|trans }}: {{ entity.createdAt ? entity.createdAt|oro_format_datetime : 'N/A' }}</li>
    <li>{{ 'oro.ui.updated_at'|trans }}: {{ entity.updatedAt ? entity.updatedAt|oro_format_datetime : 'N/A' }}</li>
{% endblock stats %}

{% block pageHeader %}
    {% set breadcrumbs = {
        'entity': entity,
        'indexPath': path('webinar_demo_ticket_index'),
        'indexLabel': 'webinar.ticket.entity_plural_label'|trans,
        'entityTitle': entity.eventName
    } %}
    {{ parent() }}
{% endblock pageHeader %}

{% block content_data %}
    {% set informationWidget %}
        {{ oro_widget_render({
            'widgetType': 'block',
            'url': path('webinar_ticket_info', {id: entity.id}),
            'title': 'Ticket Information'|trans
        }) }}
    {% endset %}

    {% set dataBlocks = [
        {
            'title': 'General Information'|trans,
            'class': 'active',
            'subblocks': [
                {'data' : [informationWidget] },
            ]
        }
    ] %}

    {% set id = 'ticketView' %}
    {% set data = {'dataBlocks': dataBlocks} %}
    {{ parent() }}
{% endblock content_data %}
