{% extends 'OroUIBundle:actions:update.html.twig' %}

{% oro_title_set({params : {"%ticket.eventName%": form.vars.value.eventName|default('N/A')} }) %}

{% set formAction = form.vars.value.id ? path('webinar_demo_ticket_update', { 'id': form.vars.value.id }) : path('webinar_demo_ticket_create')  %}

{% block head_script %}
    {{ parent() }}
    {% block stylesheets %}
        {{ form_stylesheet(form) }}
    {% endblock %}
{% endblock %}

{% block navButtons %}
    {% if form.vars.value.id and resource_granted('DELETE', form.vars.value) %}
        {{ UI.deleteButton({
            'dataUrl': path('webinar_api_delete_ticket', {'id': form.vars.value.id}),
            'dataRedirect': path('orocrm_account_index'),
            'aCss': 'no-hash remove-button',
            'dataId': form.vars.value.id,
            'id': 'btn-remove-ticket',
            'entity_label': 'webinar.ticket.entity_label'|trans,
        }) }}
        {{ UI.buttonSeparator() }}
    {% endif %}

    {{ UI.cancelButton(path('webinar_demo_ticket_index')) }}

    {% set html = UI.saveAndCloseButton() %}
    {% if form.vars.value.id or resource_granted('webinar_demo_ticket_update') %}
        {% set html = html ~ UI.saveAndStayButton() %}
    {% endif %}

    {{ UI.dropdownSaveButton({'html': html}) }}
{% endblock %}

{% block pageHeader %}
    {% if form.vars.value.id %}
        {% set breadcrumbs = {
            'entity':      form.vars.value,
            'indexPath':   path('webinar_demo_ticket_index'),
            'indexLabel': 'webinar.ticket.entity_label'|trans,
            'entityTitle': form.vars.value.eventName|default('N/A')
        }
        %}
        {{ parent() }}
    {% else %}
        {% set title = 'oro.ui.create_entity'|trans({'%entityName%': 'webinar.ticket.entity_label'|trans}) %}
        {% include 'OroUIBundle::page_title_block.html.twig' with { title: title } %}
    {% endif %}
{% endblock pageHeader %}

{% block content_data %}
    {% set id = 'ticket' %}

    {% set dataBlocks = [{
        'title': 'General'|trans,
        'class': 'active',
        'subblocks': [
            {
                'title': 'Basic Information'|trans,
                'data': [
                    form_row(form.eventName),
                    form_row(form.seatNum),
                    form_row(form.description),
                    form_row(form.eventDate),
                ]
            }
        ]
    }] %}

    {% set data = {
        'formErrors': form_errors(form)? form_errors(form) : null,
        'dataBlocks': dataBlocks,
    } %}
    {{ parent() }}
{% endblock content_data %}
